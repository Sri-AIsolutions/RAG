# RAG
This project implements a basic Retrieval-Augmented Generation (RAG) pipeline using document chunking, embeddings, vector storage, and a Large Language Model to answer queries based on custom data
